{
  "name": "midiacar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev":          "vite",
    "build":        "vite build",
    "postbuild":    "node scripts/inject-sw-version.mjs",
    "preview":      "vite preview --port 4173"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
