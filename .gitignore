# Dependências
node_modules/

# Build de produção — commitado para deploy direto no Cloudflare Pages (sem build step)
# dist/ NÃO está ignorado: executar "npm run build" antes de cada git push

# Arquivos de rascunho/backup com nomes antigos
midiacar_tablet*.html
midiacar_portal*.html
rotaads_painel*.html

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Env (caso criado futuramente)
.env
.env.*
!.env.example
